After running the SMF analyzer project, statistical information will be deposited in this folder.
Alternatively, run the compiled application specifying an input directory followed by an output directory.

The log file collects every text event encountered in scanned files.