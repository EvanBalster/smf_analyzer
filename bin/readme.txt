WINDOWS:
Run smf_analyzer.exe from the command line. It needs two arguments:
- The directory to scan (recursively) for SMF files, and
- The directory in which to output CSV data files (and the log.txt file)

MAC:
Run smf_analyzer_mac from the command line.  Same arguments.